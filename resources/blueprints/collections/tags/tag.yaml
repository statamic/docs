title: Tag
tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            import: common
            handle: title
          -
            import: page
          -
            import: common
            handle: screenshot
          -
            import: common
            handle: parameters
          -
            import: common
            handle: variables
          -
            handle: parent_tag
            field:
              display: 'Parent Tag'
              type: collections
              collection: tags
              max_items: 1
  sidebar:
    display: Sidebar
    sections:
      -
        fields:
          -
            import: common
            handle: slug
            config:
              title: {  }
              slug: {  }
