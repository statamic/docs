title: Modifier
tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            import: common
            handle: title
          -
            import: page
  sidebar:
    display: Sidebar
    sections:
      -
        fields:
          -
            import: common
            handle: slug
            config:
              title: {  }
              slug: {  }
          -
            handle: modifier_types
            field:
              type: terms
              taxonomies:
                - modifier_types
              display: 'Modifier Types'
              mode: select
