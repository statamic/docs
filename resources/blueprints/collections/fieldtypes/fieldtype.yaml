title: Fieldtype
tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            import: common
            handle: title
          -
            import: page
          -
            import: common
            handle: screenshot
          -
            import: common
            handle: screenshot_dark
  sidebar:
    display: Sidebar
    sections:
      -
        fields:
          -
            import: common
            handle: slug
            config:
              title: {  }
              slug: {  }
